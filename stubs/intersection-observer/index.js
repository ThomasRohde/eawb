// Intentionally empty. IntersectionObserver has been Baseline in all modern
// browsers since 2019, so no polyfill is needed. This stub exists so npm's
// `overrides` field can redirect the deprecated upstream package here and
// keep the install output clean.
